Где затупил:
- В форме регистрации без `event.preventDefault()` страница перезагружалась и результат пропадал.
- В TODO долго не доходило, как привязать кнопку к нужной задаче — сохранял индекс в `data-index` и брал его из `this.dataset.index`.
- В делегировании одного обработчика на контейнере хватает: через `event.target.closest(".productCard")` нахожу карточку, а по классу кнопки понимаю, что нажали.
- В корзине, чтобы не плодить дубли, перед добавлением проверяю через `find`, есть ли уже такой товар, и тогда увеличиваю `count`.
