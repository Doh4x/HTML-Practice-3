Где затупил:
- В удалении элементов сначала не понимал, как удалить именно ту задачу, на которой нажали. Помогло `this.parentElement.remove()` внутри обработчика кнопки.
- В модальном окне закрытие по клику на overlay срабатывало даже при клике по самому окну. Добавил проверку `event.target === overlay`.
- В генерации каталога важно очищать контейнер через `innerHTML = ""`, иначе при повторной отрисовке карточки дублируются.
- Состояние карточки переключаю через `classList.toggle`, а не через инлайн-стили.
